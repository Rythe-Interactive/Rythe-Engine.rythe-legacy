#version 450
#state CULL OFF
generate(vertex, fragment)

#include <stdio.shinc>


shader(vertex)
{
    void main(void)
    {
        gl_Position = ModelToScreenSpacePosition(position);
        PropagateStdIO();
    }
}

shader(fragment)
{
    uniform sampler2DArray textures;
    void main(void)
    {
        //SubmitOutput(mix(texture(textures, vec3(stdio(uv),0)),texture(textures, vec3(stdio(uv),1)),0.5));
        SubmitOutput(texture(textures, vec3(stdio(uv),0)));
    }
}